# Maintain Console

面向分布式应用的远程脚本运维与自动化工作台。所有登录用户从同一目录进入脚本工作台，具体能力由脚本授权决定。

## 领域语言

**工具**：当前保存的脚本、参数定义、用途说明、所属应用与允许使用范围的整体。
_避免_：任务、发布物。

**草稿**：开发者尚未保存的代码、参数定义和说明，不影响其他人正在使用的工具。

**工具导入文档**：只包含工具名称、说明、脚本、参数定义与用途风险的版本化 JSON；应用、目录、环境和脚本授权由导入者在脚本工作台中决定。
_避免_：导入权限包、工具更新包。

**工具运行**：使用者运行自己看到的已保存工具版本，仅提供参数值和允许的目标选择。

**草稿调试**：同时具备编辑与执行权限的开发者运行未保存内容，不自动保存。

**授权分享**：具备脚本授权管理权限的用户配置权限，再复制工作台入口；链接本身不授予权限，也不触发运行。

**脚本工作台**：查看、运行和编辑脚本的统一产品入口；开发者与运营使用同一页面，页面按脚本授权裁剪能力。
_避免_：开发者专属页面、工具运行页

**管理端**：独立的账号、运行概览和环境管理页面；管理角色只决定管理端功能范围，不授予任何脚本能力。
_避免_：脚本超级管理员

**目录可见**：登录用户可以看到目录和脚本名称；不代表可以读取源码、运行、编辑或管理授权。

**目录能力提示**：目录按当前登录用户展示某个脚本可查看、可编辑、可运行、可授权或仅可见的判定结果；它不是权限配置，
不返回授权主体名单，也不授予任何新能力。
_避免_：公开权限、脚本角色

**查看代码／编辑脚本／运行工具**：分别允许查看源码、修改工具内容、执行工具；授权管理不等同于编辑脚本。

**执行记录**：一次运行已观察到的事实，包括执行者、版本、目标、耗时与结果；不承担调度、恢复或重试。

**结果未知**：调用已发出但未获得可信的最终结果，不能推断业务操作已停止或未发生。

**操作类型**：作者提供的查询类或操作类用途标识，不是只读证明或安全隔离。

**本地账号**：
用户名密码凭证与一个系统用户的绑定；用户名不可变，密码只保存安全哈希。
_避免_：脚本角色、员工姓名

**认证来源**：
负责验证一次登录凭证并给出可信身份；当前默认是本地账号，未来公司 SDK 接入时也不保存脚本权限。
_避免_：用户、账号角色

**系统用户**：
Maintain Console 对一个可信外部身份的本地记录，拥有独立的启用状态和系统角色；既有 `employeeNo` 继续作为脚本 ACL 的稳定主体标识。
_避免_：登录凭证、会话

**登录会话**：
一次认证成功后建立的短期访问状态，只引用系统用户；每次受保护请求仍以系统用户的当前状态和角色为准。
_避免_：用户、长期令牌

**管理角色**：
圈定管理端账号、运行概览和环境等功能范围；不会隐式获得任何脚本权限。
_避免_：开发者角色、脚本授权、读编执权限

**脚本授权**：
脚本 permissions JSON 中对查看、编辑、运行和授权管理的显式分配；系统管理角色、白名单和页面入口都不能旁路它。
_避免_：系统角色、目录公开状态
